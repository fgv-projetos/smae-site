services:
  smae-site:
    build:
      context: .
      dockerfile: Dockerfile
    image: smae-site:latest
    container_name: smae-site-dev
    ports:
      - "80:80"
      - "443:443"
    restart: unless-stopped
    environment:
      # teste como se fosse produção
      - NODE_ENV=production
      - NITRO_PORT=3000
      - NITRO_HOST=127.0.0.1
      # Email configuration (override defaults from Dockerfile)
      - NUXT_EMAIL_DRIVER=fgv
      - NUXT_EMAIL_HOST=smtpapp.fgv.br
      - NUXT_EMAIL_PORT=25
      - NUXT_EMAIL_USER=
      - NUXT_EMAIL_PASS=
    volumes:
      # Certificados SSL - serão lidos do diretório ./certs/
      - ./certs:/etc/nginx/ssl:ro
    healthcheck:
      test:
        [
          "CMD",
          "curl",
          "-f",
          "-k",
          "https://localhost/health",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - smae-network
    labels:
      - "com.smae.description=SMAE Site - Development"
      - "com.smae.environment=development"

networks:
  smae-network:
    driver: bridge